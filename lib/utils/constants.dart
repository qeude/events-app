import 'package:flutter/material.dart';
// Text
const appTitle = 'Events';
const errorMessage = 'Something went wrong. Try again!';
const emptyList = 'Event list is empty !';
const eventNameTextStyle = TextStyle(color: Colors.white, fontSize: 50.0, shadows: [Shadow(color: Colors.black12, blurRadius: 4.0)]);
const eventDateTextStyle = TextStyle(color: Colors.white, fontSize: 20.0, shadows: [Shadow(color: Colors.black12, blurRadius: 4.0)]);

// Numbers
const opacityVisible = 1.0;
const opacityInvisible = 0.0;
const defaultAnimationDuration = 500;